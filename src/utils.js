export const findCustomDiv = (input) => {
  let answer = '';

  for (let i = 0; i < input.length; i++) {
    const str = input[i];
  }

  return answer;
};

export const splitString = (input, div) => {
  let answer = [];
  return answer;
};
